services:
  stt-to-notion:
    image: ${STT_IMAGE}
    container_name: stt-to-notion
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
